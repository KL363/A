<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.agentback.mapper.RegisterMapper">
    <insert id="InsertUser">
        insert into myuser (userName, userPassword, userEmail) values (#{userName},#{userPassword},#{userEmail});
    </insert>
    <select id="SelectUserByEmail" resultType="integer">
        select id from myuser
        <where>
            userEmail = #{userEmail} or userName = #{userName}
        </where>
    </select>
</mapper>