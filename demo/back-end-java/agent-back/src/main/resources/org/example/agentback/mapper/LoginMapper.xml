<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.agentback.mapper.LoginMapper">
    <select id="SelectByNameAndPassword" resultType="integer">
        select id from myuser
        <where>
            userName = #{userName} and userPassword = #{userPassword};
        </where>
    </select>
    <select id="SelectByEmail" resultType="integer">
        select id from myuser where userEmail = #{userEmail}
    </select>
    <select id="SelectById" resultType="integer">
        select historyId from userhistory where userId = #{userId} order by historyId desc;
    </select>
</mapper>